#include<iostream>
using namespace std;

class Num{
	private:
		float num;
		int result;
	public:
		void wholenumber(double);
		void pos_or_neg(double);
		void actual_fac(double);
		void display();
};

void Num::wholenumber(double num)
{
	if (num>=0)
	    {
	    	cout<<"num is whole number";
		}else:
			cout<<"Invalid input"; 
}

void Num::pos_or_neg(double num)
{
	
	
}


